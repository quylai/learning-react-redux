import React, { Component } from "react";
import "./App.css";
import BlahComp from "./components/BlahComp";


class App extends Component {
  
  render() {
    return(
      <div className="App">
        
      </div>
    );
  }
}
export default App;

/*
new user form (form with username, email, password), new user submit form button (button tag), "create reel" button (button tag), "view saved reels" button (button tag), "find new reel" button (button tag)
*/
